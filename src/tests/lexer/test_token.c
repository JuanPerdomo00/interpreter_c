#include "lexer/token.h"
#include <assert.h>
#include <stdio.h>
#include <string.h>

int main() {
  
  // Token t = token_create(IDENTIFIER, "x",1,0);

  // assert(strcmp(t.data, "x") == 0);
  // printf("paso los test");
  return 0;
}
